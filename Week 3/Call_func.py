from Temp_func import temperature_conversion # Import the function from the Temp_func.py file

Temp = (input("What's the Temperature? Include the unit of measurement. \n"))

temperature_conversion(Temp)# Call the function from the void
# This will run the function from the Temp_func.py file.
